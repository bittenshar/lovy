╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║            🧪 OneSignal Postman Testing - Quick Start 🧪            ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

📁 COLLECTION FILES
═══════════════════════════════════════════════════════════════════════

• postman/OneSignal-Complete-Testing.postman_collection.json
  → Import this file into Postman
  → Contains 15+ pre-configured test requests
  → Includes automated test scripts

📖 TESTING GUIDES
═══════════════════════════════════════════════════════════════════════

• POSTMAN_TESTING_GUIDE.md
  → Complete testing workflow
  → Scenario walkthroughs
  → Troubleshooting tips
  → Performance testing

🚀 QUICK START (5 Minutes)
═══════════════════════════════════════════════════════════════════════

1. Open Postman
2. Click "Import" → Select collection file
3. Collection appears in left sidebar
4. Set baseUrl variable (default: http://localhost:3000)
5. Run "Login - Worker" request
6. Run "Register OneSignal ID" request
7. Run "Test Notification" request
8. ✅ Receive notification on device!

📂 FOLDER STRUCTURE
═══════════════════════════════════════════════════════════════════════

🔐 Authentication (2 requests)
   ├─ Login - Worker
   └─ Login - Employer

📱 Device Registration (4 requests)
   ├─ Register OneSignal ID (iOS)
   ├─ Register OneSignal ID (Android)
   ├─ Get OneSignal Status
   └─ Unregister OneSignal ID

📨 Send Notifications (5 requests)
   ├─ Test Notification
   ├─ Send to Specific Users
   ├─ Send to All Workers
   ├─ Send to All Employers
   └─ (Broadcast Variants)

⏰ Advanced Features (2 requests)
   ├─ Schedule Notification
   └─ Get Notification Status

🧪 Test Scenarios (2 comprehensive flows)
   ├─ Complete Flow: Register + Send + Check
   └─ Multi-User Notification Campaign

🔑 ENVIRONMENT VARIABLES
═══════════════════════════════════════════════════════════════════════

Manual Setup:
  baseUrl          = http://localhost:3000
  auth_token       = (auto-filled by login)
  employer_token   = (auto-filled by login)
  user_id          = (auto-filled by login)
  onesignal_id     = (auto-filled by registration)

Auto-Populated By Requests:
  ✅ Login → Sets auth_token, user_id
  ✅ Register → Sets onesignal_id
  ✅ Send → Sets last_notification_id

📊 TEST FEATURES
═══════════════════════════════════════════════════════════════════════

✅ Automated Test Scripts
   - Verify response status (200)
   - Check required fields
   - Validate data types
   - Assert correct values

✅ Console Logging
   - Readable output with emojis
   - Variable tracking
   - Success/failure indicators
   - Helpful debugging info

✅ Auto-Variable Capture
   - Tokens saved automatically
   - Device IDs captured
   - Notification IDs stored
   - No manual copying needed

✅ Pre-configured Requests
   - Ready to use examples
   - Real-world test data
   - Custom fields for jobs
   - Priority levels set

🎯 TESTING WORKFLOW
═══════════════════════════════════════════════════════════════════════

Step 1: Authenticate
  Request: Login - Worker
  ↓
  ✅ Token saved to auth_token
  ✅ User ID saved to user_id

Step 2: Register Device
  Request: Register OneSignal ID (iOS)
  ↓
  ✅ Device registered
  ✅ OneSignal ID saved

Step 3: Verify Registration
  Request: Get OneSignal Status
  ↓
  ✅ Confirms device registered
  ✅ Shows platform and timestamp

Step 4: Send Notification
  Request: Test Notification
  ↓
  ✅ Notification sent
  ✅ Check device for message

Step 5: Check Analytics (Optional)
  Request: Get Notification Status
  ↓
  ✅ Shows delivery metrics
  ✅ Displays engagement data

⏱️ TIME ESTIMATES
═══════════════════════════════════════════════════════════════════════

Authentication      2 min  (1-2 requests)
Device Registration 3 min  (3-4 requests)
Send Notification   5 min  (5-8 requests)
Advanced Tests      5 min  (9-10 requests)
                    ─────────────
Total Setup        ~15 min (10 requests)

✨ REQUEST EXAMPLES
═══════════════════════════════════════════════════════════════════════

Login as Worker:
  POST /api/auth/login
  Body: {"email": "worker@example.com", "password": "password123"}

Register Device:
  POST /api/notifications/onesignal/register
  Header: Authorization: Bearer {{auth_token}}
  Body: {"onesignalId": "device-id", "platform": "iOS"}

Send Test Notification:
  POST /api/notifications/onesignal/test
  Header: Authorization: Bearer {{auth_token}}

Send to Workers:
  POST /api/notifications/onesignal/send-to-workers
  Header: Authorization: Bearer {{employer_token}}
  Body: {"title": "Title", "message": "Message"}

Send to Specific Users:
  POST /api/notifications/onesignal/send
  Header: Authorization: Bearer {{auth_token}}
  Body: {
    "title": "Title",
    "message": "Message",
    "users": ["user-id-1"],
    "data": {"custom": "data"}
  }

Schedule Notification:
  POST /api/notifications/onesignal/schedule
  Header: Authorization: Bearer {{auth_token}}
  Body: {
    "title": "Title",
    "message": "Message",
    "scheduledTime": "2024-12-15T10:00:00Z",
    "users": ["user-id"]
  }

Check Status:
  GET /api/notifications/onesignal/{{last_notification_id}}/status
  Header: Authorization: Bearer {{auth_token}}

🎓 TEST EXECUTION TIPS
═══════════════════════════════════════════════════════════════════════

Tip 1: Run Requests Sequentially
  - Don't skip steps
  - Later requests need earlier data
  - Variables must be set

Tip 2: Check Console Output
  - Click Console (bottom-left)
  - See detailed logs
  - Track variable changes

Tip 3: View Test Results
  - Click "Tests" tab after request
  - Green ✅ = Passed
  - Red ❌ = Failed
  - See assertions

Tip 4: Inspect Responses
  - "Pretty" tab shows formatted JSON
  - "Raw" tab shows raw response
  - Check Headers for status codes

Tip 5: Debug Variables
  - Click "Variables" tab
  - See all current values
  - Manually edit if needed

Tip 6: Create Variations
  - Right-click request → Duplicate
  - Modify data
  - Save as new request

🐛 COMMON ISSUES & FIXES
═══════════════════════════════════════════════════════════════════════

Issue: 401 Unauthorized
  ❌ auth_token not set in header
  ✅ Fix: Run "Login - Worker" first

Issue: 400 Bad Request
  ❌ Invalid JSON in body
  ✅ Fix: Check JSON syntax

Issue: 404 Not Found
  ❌ Incorrect endpoint path
  ✅ Fix: Verify baseUrl and path

Issue: 500 Server Error
  ❌ Server issue or validation error
  ✅ Fix: Check server logs

Issue: Notification Not Received
  ❌ Device not registered
  ❌ Permissions not granted
  ✅ Fix: Check status, verify permissions

✅ SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════════════

✅ Authentication
   □ Login returns 200
   □ Token is saved to auth_token
   □ User ID is populated

✅ Registration
   □ Registration returns 200
   □ Status shows "registered: true"
   □ OneSignal ID is captured

✅ Notification Sending
   □ Test notification returns 200
   □ Status is "success"
   □ Notification appears on device

✅ Broadcasts
   □ Send to workers returns 200
   □ Send to employers returns 200
   □ Message appears on devices

✅ Advanced Features
   □ Schedule returns 200
   □ Status check works
   □ Analytics data shown

📞 NEED HELP?
═══════════════════════════════════════════════════════════════════════

Full Documentation:   POSTMAN_TESTING_GUIDE.md
API Reference:        ONESIGNAL_INTEGRATION.md
Setup Instructions:   ONESIGNAL_SETUP.md
Quick Reference:      ONESIGNAL_QUICK_REFERENCE.txt

═══════════════════════════════════════════════════════════════════════

Version: 1.0 | Status: Production Ready ✅

Ready to test? Import the collection and follow the workflow above!

═══════════════════════════════════════════════════════════════════════
